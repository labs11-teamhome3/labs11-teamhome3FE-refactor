import React from 'react';

const styles = theme => ({
  paper: {
    'max-width': '800px',
    margin: '0 auto',
    'text-align': 'left',
    padding: '20px',
  },
});

const ViewEventModal = props => {
  return <div />;
};

export default ViewEventModal;
